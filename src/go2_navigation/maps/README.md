# Chapter 12 Maps

Put the chapter-11 saved map files here if you want to use the default workspace path.

Expected files:

- `my_map.yaml`
- `my_map.pgm`

You can also keep the map anywhere else and pass it explicitly:

```bash
ros2 launch go2_navigation navigation.launch.py map:=/absolute/path/to/my_map.yaml
```
